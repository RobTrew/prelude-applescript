{
  "title": "lcm",
  "cells": [
    {
      "type": "code",
      "language": "applescript",
      "data": "-- lcm :: Int -> Int -> Int\non lcm(x, y)\n    if (x = 0 or y = 0) then\n        0\n    else\n        abs(floor(x / (gcd(x, y))) * y)\n    end if\nend lcm"
    }
  ]
}