{
  "title": "lookup",
  "cells": [
    {
      "type": "code",
      "language": "javascript",
      "data": "-- use framework \"Foundation\"\n-- use scripting additions"
    },
    {
      "type": "code",
      "language": "javascript",
      "data": "-- lookup :: Eq a => a -> Container -> Maybe b\ron lookup(k, m)\r\tset c to class of m\r\tif c is list then\r\t\tlookupTuples(k, m)\r\telse if c = record then\r\t\tlookupDict(k, m)\r\telse\r\t\tNothing()\r\tend if\rend lookup"
    }
  ]
}