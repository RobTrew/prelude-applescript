{
  "title": "pred",
  "cells": [
    {
      "type": "code",
      "language": "applescript",
      "data": "-- pred :: Enum a => a -> a\non pred(x)\n    if isChar(x) then\n        chr(ord(x) - 1)\n    else\n        (-1) + x\n    end if\nend pred"
    }
  ]
}